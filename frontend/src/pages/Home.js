import React from 'react';
import Landing from '../components/home/Landing';

const Home = () => {
    return (
        <section id='home-page'>
            <Landing />
        </section>
    );
};

export default Home;